<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Story Time | Edit</title>

    <link rel='stylesheet' href='/public/styles/global.css'>
    <link rel="stylesheet" href="/public/styles/newConnection.css">
</head>
<body>
    <%- include('../components/header') %>

    <main>
        <!--topic, title, host, details, where, when, start, end, image url-->
        <form action="/connection/edit/<%= connection.connection_id %>" method="post">
            <h2>Edit Connection</h2>

            <label>Title</label>
            <input type="text" placeholder="title" name="connection_name" value="<%= connection.connection_name %>">

            <label>Topic</label>
            <input type="text" placeholder="topic" name="topic" value="<%= connection.topic %>">
            
            <label>Host</label>
            <input type="text" name="host_name" placeholder="host" value="<%= connection.host_name %>">

            <label>Details</label>
            <textarea name="details"><%= connection.details %></textarea>

            <label>Location</label>
            <input type="text" placeholder="location" name="location" value="<%= connection.location %>">

            <label>Date</label>
            <input type="date" name="date" value="<%= connection.date %>">

            <label>Start</label>
            <input type="time" name="start_time" value="<%= connection.start_time %>">

            <label>End</label>
            <input type="time" name="end_time" value="<%= connection.end_time %>">

            <label>Image URL</label>
            <input type="text" placeholder="image url" name="img" value="<%= connection.img %>">
            
            <input type="submit" class="button">
        </form>
    </main>

    <%- include('../components/footer') %>
</body>
</html>