<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Story Time</title>

    <link rel='stylesheet' href='/public/styles/global.css'>
    <link rel="stylesheet" href="/public/styles/connection.css">
</head>
<body>
    <%- include('../components/header') %>

    <main>
        <!--
        <h2><%= connection.connection_name %></h2>


        <div class="flex">
            <a class="button" href="/connection/edit/<%= connection.connection_id %>">Edit</a>
            <a class="button" href="/connection/delete/<%= connection.connection_id %>">Delete</a>
        </div>

        <div class="info">
            <p>Hosted by <em><%= connection.host_name %></em></p>
            <p><%= connection.date %></p>
            <p><%= connection.start_time %> - <%= connection.end_time %></p>
            <p><%= connection.location %></p>
        </div>

        <img src="<%= connection.img %>" alt="<%= connection.location %>">

        

        <div class="details">
            <h3>Details</h3>
            <p><%= connection.details %></p>
        </div>
        -->
    </main>

    <%- include('../components/footer') %>
</body>
</html>